import { Metadata } from 'next';
import React from 'react';

export const metadata : Metadata = {
    title: 'Settings - Bug Tracker',
    description: 'Settings page'
    
};

const SettingsPage = () => {
  return (
    <div>SettingsPage</div>
  )
}

export default SettingsPage